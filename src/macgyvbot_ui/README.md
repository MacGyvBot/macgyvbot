# macgyvbot_ui

User interface package.

## Planned Scope

- command/status GUI widgets
- camera/status panels
- UI presenters

This package can remain folded into `macgyvbot_command` until the UI grows.
