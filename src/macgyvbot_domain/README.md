# macgyvbot_domain

Shared Python dataclasses used between migrated MacGyvBot packages.

## Scope

- `PickTarget`
- `PickMotionPlan`

Keep ROS transport contracts in `macgyvbot_interfaces`. This package is for
in-process Python workflow models only.
